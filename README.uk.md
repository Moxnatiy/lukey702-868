# Lukey 702 / 868 — відкрита прошивка

Реверс-інжиніринг і переписана відкрита прошивка для паяльної станції **Lukey 702**
(на платі маркування **Lukey 868**), SMD rework. У станції **два мікроконтролери**
(обидва ATmega8): один керує **феном (гарячим повітрям)**, інший — **паяльником**.

> 🇬🇧 [Read in English](README.md)

Оригінальна прошивка була заблокована від читання (read-protected) і працювала некоректно,
тож проєкт відтворює прошивку кожного МК з нуля — розводку пінів відновлено зі схеми та
референсної прошивки, далі чиста задокументована реалізація на C із меню калібрування
прямо на станції.

## Структура репозиторію

| Тека | МК | Стан |
|------|-----|------|
| [`hot-air/`](hot-air/) | Фен — ATmega8L | ✅ працює: PID + авто-тюн, меню CAL на станції, прошито й протестовано |
| [`solder-iron/`](solder-iron/) | Паяльник — ATmega8 | 🚧 заплановано |

Кожен підпроєкт самодостатній (код, документація, Makefile, конфіг VS Code).

## Прошивка фена — можливості

- Повний кінцевий автомат **standby → робота → охолодження** за офіційним 10-пунктним алгоритмом.
- **PID**-керування нагрівом (P/I/d) на 20 Гц, налаштоване під інерційну термопару у фені.
- **Меню калібрування прямо на станції** (утримати **UP** при вмиканні → `CAL`), без перекомпіляції:
  - правка `P` / `I` / `d`, зсуву ADC→°C (`OF`) та температури вимкнення (`LO`);
  - `TST` — самотест індикатора (світить усі сегменти);
  - **`AT` — автоналаштування PID в один дотик** (релейний метод Åström–Hägglund @250 °C).
- Якісний АЦП (прескалер /128, 16× усереднення, оновлення 20 Гц).
- **Індикація крапками:** усі три крапки світять постійно, поки фен на підставці; під час нагріву
  остання крапка блимає пропорційно потужності ШІМ (швидко = повна потужність).
- Уставка **100–480 °C**, зберігається в EEPROM; fuse EESAVE зберігає калібрування при перепрошивці.

Готова прошивка прикріплена до кожного [релізу](https://github.com/Moxnatiy/lukey702-868/releases).

## Залізо

- **МК:** ATmega8L-8PU, внутрішній RC 8 МГц (без зовнішнього кварцу).
- **Плата:** Scorpio «LUKEY868(702) SMD REWORK».
- **Програматор:** USBASP (ISP). Прошивка через `avrdude`.
- **Тулчейн:** `avr-gcc` (зібрано тулчейном AVR від PlatformIO, gcc 7.3.0).

## Швидкий старт (фен)

```bash
cd hot-air
make            # зібрати -> fan.hex
make flash      # chip-erase + прошивка через USBASP
```

Деталі, карта пінів і меню калібрування — у [`hot-air/README.md`](hot-air/README.md).

## Подяки

- Схема: **Scorpio** (LUKEY868/702 SMD rework).
- Референсна прошивка для відновлення розводки: **Luk702_fan_SE-2**, автор *Volly*
  (форум radiokot.ru). Див. `hot-air/original/`.
- Реверс-інжиніринг і нова прошивка: **Serhii ([@Moxnatiy](https://github.com/Moxnatiy))**.

## Ліцензія

Наш код (прошивка в `*/src`, інструменти, Makefile, документація) — під
[ліцензією MIT](LICENSE). Сторонні матеріали (бінарник референсної прошивки та схема)
належать їхнім авторам і включені лише для сумісності й документації.
